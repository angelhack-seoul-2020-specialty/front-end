import styled from 'styled-components';

const IconButton = styled.button`
  display: inline-flex;
  background-color: transparent;
  border: none;
  font-size: 1.65em;
  cursor: pointer;
`;

export { IconButton }
